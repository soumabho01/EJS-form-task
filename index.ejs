<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
   <div class="main w-full min-h-screen bg-zinc-800">
    <div class="form p-10">
        <form action="/addTask" method="POST">
            <input class="block w-full px-5 py-2 bg-zinc-800 text-white" type="text" name="name" id="name" placeholder="Write your name here..." required>
            <textarea class="block w-full mt-2 px-5 py-2 bg-zinc-800 text-white" name="details" id="details" placeholder="Write task details here..." required></textarea>
            <input type="submit" value="Create Task" class="mt-2 bg-blue-800 text-white px-5 py-2 cursor-pointer">
        </form>
    </div>
    
    <div class="tasks flex gap-3 flex-wrap p-10">
        <% tasks.forEach((task, index) => { %>
            <div class="w-72 px-3 py-4 bg-zinc-800 rounded-md">
                <h1 class="text-white text-2xl"><%= task.name %></h1>
                <a href="/task/<%= index %>" class="text-blue-800 mt-2 block">Read more...</a>
            </div>
        <% }); %>
    </div>
   </div>
</body>
</html>

